---
phase: 02-dashboard-shell-kpi-cards
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kpi-utils.ts
  - components/dashboard/kpi-cards-skeleton.tsx
autonomous: true
requirements: [KPI-02, KPI-03, KPI-04, KPI-05, UX-01]
must_haves:
  truths:
    - "shadcn Tabs, Progress, Skeleton 컴포넌트가 components/ui/ 에 생성된다"
    - "lib/kpi-utils.ts에서 달성률, 델타, 포맷팅 함수를 export한다"
    - "KpiCardsSkeleton이 5개 카드 형태의 스켈레턴을 렌더링한다"
  artifacts:
    - path: "lib/kpi-utils.ts"
      provides: "KPI 계산/포맷팅 유틸리티"
      exports: [calcAchievementRate, calcDelta, formatKpiValue, getAchievementColorClass, getProgressColorClass, getDeltaColorClass, formatDelta]
    - path: "components/dashboard/kpi-cards-skeleton.tsx"
      provides: "Suspense fallback 스켈레턴 (5개 카드)"
      contains: "KpiCardsSkeleton"
    - path: "components/ui/tabs.tsx"
      provides: "shadcn Tabs 컴포넌트"
    - path: "components/ui/progress.tsx"
      provides: "shadcn Progress 컴포넌트"
    - path: "components/ui/skeleton.tsx"
      provides: "shadcn Skeleton 컴포넌트"
  key_links:
    - from: "lib/kpi-utils.ts"
      to: "components/dashboard/kpi-card.tsx"
      via: "import in Phase 02-02"
      pattern: "calcAchievementRate|calcDelta|formatKpiValue"
    - from: "components/ui/skeleton.tsx"
      to: "components/dashboard/kpi-cards-skeleton.tsx"
      via: "import { Skeleton }"
      pattern: "Skeleton"
---

<objective>
shadcn 컴포넌트 설치(Tabs, Progress, Skeleton)와 KPI 계산/포맷팅 유틸리티를 구현하고, Suspense 폴백용 스켈레턴 컴포넌트를 생성한다.

Purpose: Phase 02-02에서 KPI 카드와 탭 컴포넌트가 의존하는 shadcn UI 파일, 계산 로직, 스켈레턴을 선행 준비한다.
Output: components/ui/tabs.tsx, components/ui/progress.tsx, components/ui/skeleton.tsx, lib/kpi-utils.ts, components/dashboard/kpi-cards-skeleton.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-layer-foundation/01-02-SUMMARY.md
@types/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn Tabs, Progress, Skeleton 컴포넌트 설치</name>
  <files>
    components/ui/tabs.tsx
    components/ui/progress.tsx
    components/ui/skeleton.tsx
  </files>
  <action>
    다음 명령어를 순서대로 실행한다:

    ```bash
    npx shadcn@latest add tabs --yes
    npx shadcn@latest add progress --yes
    npx shadcn@latest add skeleton --yes
    ```

    각 명령 후 components/ui/ 디렉터리에 해당 파일이 생성되었는지 확인한다.
    설치 후 `npm run build` 또는 `npm run lint`로 새 파일의 TypeScript 오류가 없는지 확인한다.
    단, 기존 page.tsx의 getDashboardData import 오류는 Phase 2 전체 완료 전까지 허용된다.
  </action>
  <verify>
    - `ls components/ui/tabs.tsx components/ui/progress.tsx components/ui/skeleton.tsx` 세 파일 모두 존재
    - `npm run lint 2>&1 | grep -E "components/ui/(tabs|progress|skeleton)"` — 새 파일에 ESLint 오류 없음
  </verify>
  <done>components/ui/tabs.tsx, progress.tsx, skeleton.tsx 세 파일이 모두 존재하고 import 가능한 상태</done>
</task>

<task type="auto">
  <name>Task 2: KPI 계산/포맷팅 유틸리티 구현 (lib/kpi-utils.ts)</name>
  <files>lib/kpi-utils.ts</files>
  <action>
    `lib/kpi-utils.ts` 파일을 새로 생성한다. 아래 함수들을 named export로 구현한다.

    **달성률 계산 (Weekly 탭 전용 — DailyRecord에는 목표값 없음):**
    ```typescript
    /** 달성률(%) 계산. target이 0이면 0 반환 */
    export function calcAchievementRate(actual: number, target: number): number {
      if (target === 0) return 0;
      return Math.min(Math.round((actual / target) * 100), 999); // 999% 상한
    }
    ```

    **기간 비교 델타 계산:**
    ```typescript
    /** 현재값과 직전값의 델타를 계산한다. previous가 0이면 percent=0 */
    export function calcDelta(current: number, previous: number): { percent: number; absolute: number } {
      if (previous === 0) return { percent: 0, absolute: current };
      const absolute = current - previous;
      const percent = Math.round((absolute / Math.abs(previous)) * 100);
      return { percent, absolute };
    }
    ```

    **달성률 색상 클래스 (KPI-05: 80%+ 녹색, 60~80% 주황, 60% 미만 빨간):**
    ```typescript
    /** 달성률에 따른 텍스트 색상 Tailwind 클래스 반환 */
    export function getAchievementColorClass(rate: number): string {
      if (rate >= 80) return 'text-green-600 dark:text-green-400';
      if (rate >= 60) return 'text-orange-500 dark:text-orange-400';
      return 'text-red-600 dark:text-red-400';
    }

    /** 달성률에 따른 Progress 바 색상 Tailwind 클래스 반환 */
    export function getProgressColorClass(rate: number): string {
      if (rate >= 80) return '[&>div]:bg-green-500';
      if (rate >= 60) return '[&>div]:bg-orange-400';
      return '[&>div]:bg-red-500';
    }
    ```

    **델타 색상 (오르면 녹색, 내리면 빨간 — CONTEXT.md 결정):**
    ```typescript
    /** 델타 방향에 따른 텍스트 색상 Tailwind 클래스 반환 */
    export function getDeltaColorClass(percent: number): string {
      return percent >= 0
        ? 'text-green-600 dark:text-green-400'
        : 'text-red-600 dark:text-red-400';
    }
    ```

    **델타 포맷팅 ("▲ 12% / ₩120만" 형식 — CONTEXT.md 결정):**
    ```typescript
    /**
     * 델타를 "▲ +12% / ₩120만" 형식으로 포맷팅한다.
     * 금액이 아닌 KPI(이용건수, 가동률, 이용시간)는 showWon=false로 호출한다.
     */
    export function formatDelta(
      percent: number,
      absolute: number,
      unit: '원' | '건' | '%' | '시간'
    ): string {
      const arrow = percent >= 0 ? '▲' : '▼';
      const sign = percent >= 0 ? '+' : '';
      if (unit === '원') {
        const absWon = Math.abs(absolute);
        return `${arrow} ${sign}${percent}% / ₩${(absWon / 10000).toLocaleString()}만`;
      }
      if (unit === '%') {
        return `${arrow} ${sign}${percent}% / ${Math.abs(absolute).toFixed(1)}%p`;
      }
      if (unit === '시간') {
        return `${arrow} ${sign}${percent}% / ${Math.abs(absolute).toLocaleString()}시간`;
      }
      // 건수
      return `${arrow} ${sign}${percent}% / ${Math.abs(absolute).toLocaleString()}건`;
    }
    ```

    **KPI 값 포맷팅 (CLAUDE.md 규칙: ₩${amount/10000}만, 이용시간=N시간, 이용건수=N건, 가동률=N%):**
    ```typescript
    /** KPI 종류에 따라 표시 문자열 반환 */
    export function formatKpiValue(
      value: number,
      unit: '원' | '건' | '%' | '시간'
    ): string {
      if (unit === '원') return `₩${(value / 10000).toLocaleString()}만`;
      if (unit === '%') return `${value.toFixed(1)}%`;
      if (unit === '시간') return `${value.toLocaleString()}시간`;
      return `${value.toLocaleString()}건`;
    }
    ```

    파일 최상단에 Korean 주석으로 모듈 목적을 설명한다. 모든 함수에 JSDoc 한국어 주석 포함.
  </action>
  <verify>
    `npx tsc --noEmit --skipLibCheck 2>&1 | grep "kpi-utils"` — kpi-utils.ts TypeScript 오류 없음
  </verify>
  <done>lib/kpi-utils.ts가 존재하고 7개 함수가 named export로 구현됨. TypeScript 컴파일 오류 없음</done>
</task>

<task type="auto">
  <name>Task 3: KpiCardsSkeleton 컴포넌트 구현</name>
  <files>components/dashboard/kpi-cards-skeleton.tsx</files>
  <action>
    `components/dashboard/kpi-cards-skeleton.tsx` 파일을 생성한다. React Suspense의 fallback으로 사용할 5개 카드 형태 스켈레턴이다.

    ```typescript
    // KPI 카드 로딩 스켈레턴 — Suspense fallback으로 사용
    import { Card, CardContent, CardHeader } from '@/components/ui/card';
    import { Skeleton } from '@/components/ui/skeleton';

    export function KpiCardsSkeleton() {
      return (
        <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">
          {Array.from({ length: 5 }).map((_, i) => (
            <Card key={i}>
              <CardHeader className="pb-2">
                {/* KPI 제목 스켈레턴 */}
                <Skeleton className="h-4 w-20" />
              </CardHeader>
              <CardContent className="space-y-3">
                {/* 실적값 스켈레턴 */}
                <Skeleton className="h-7 w-28" />
                {/* 프로그레스 바 스켈레턴 */}
                <Skeleton className="h-2 w-full" />
                {/* 달성률 + 델타 스켈레턴 */}
                <Skeleton className="h-3 w-24" />
                <Skeleton className="h-3 w-32" />
              </CardContent>
            </Card>
          ))}
        </div>
      );
    }
    ```

    그리드 클래스는 KpiCards(02-02에서 구현)와 동일한 반응형 그리드를 사용해야 한다:
    `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5`
  </action>
  <verify>
    `npx tsc --noEmit --skipLibCheck 2>&1 | grep "kpi-cards-skeleton"` — TypeScript 오류 없음
  </verify>
  <done>components/dashboard/kpi-cards-skeleton.tsx가 존재하고 KpiCardsSkeleton 컴포넌트가 export됨</done>
</task>

</tasks>

<verification>
- `ls components/ui/tabs.tsx components/ui/progress.tsx components/ui/skeleton.tsx` — 3개 파일 존재
- `ls lib/kpi-utils.ts components/dashboard/kpi-cards-skeleton.tsx` — 2개 파일 존재
- `npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "kpi-utils|kpi-cards-skeleton"` — 해당 파일 TypeScript 오류 없음
</verification>

<success_criteria>
- shadcn Tabs, Progress, Skeleton 컴포넌트가 components/ui/ 에 설치됨
- lib/kpi-utils.ts에서 calcAchievementRate, calcDelta, formatDelta, formatKpiValue, getAchievementColorClass, getProgressColorClass, getDeltaColorClass 7개 함수가 export됨
- KpiCardsSkeleton이 5개 카드 형태의 스켈레턴을 렌더링하는 컴포넌트로 export됨
- 새 파일들에 TypeScript 컴파일 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/02-dashboard-shell-kpi-cards/02-01-SUMMARY.md`를 생성한다.
</output>
