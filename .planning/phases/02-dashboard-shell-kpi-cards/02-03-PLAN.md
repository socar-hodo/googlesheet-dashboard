---
phase: 02-dashboard-shell-kpi-cards
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/(dashboard)/dashboard/page.tsx
autonomous: false
requirements:
  - TAB-02
  - TAB-03
  - UX-01
must_haves:
  truths:
    - "대시보드 페이지가 getDashboardData 대신 getTeamDashboardData()로 데이터를 가져온다"
    - "export const dynamic = 'force-dynamic'가 설정되어 탭 전환 시 서버에서 최신 데이터를 가져온다"
    - "URL ?tab=daily 또는 ?tab=weekly에 따라 KpiCards가 해당 탭 데이터를 렌더링한다"
    - "탭 전환 시 Suspense key가 변경되어 KpiCardsSkeleton이 재표시된다"
    - "npm run build가 성공한다 (Phase 1에서 허용했던 getDashboardData import 오류가 해소됨)"
  artifacts:
    - path: "app/(dashboard)/dashboard/page.tsx"
      provides: "대시보드 메인 페이지 (searchParams 기반 탭 분기)"
      contains: "export const dynamic = 'force-dynamic'"
      exports: [DashboardPage]
  key_links:
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "lib/data.ts"
      via: "getTeamDashboardData()"
      pattern: "getTeamDashboardData"
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/kpi-cards.tsx"
      via: "<KpiCards data={data} tab={activeTab} />"
      pattern: "KpiCards.*tab="
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/kpi-cards-skeleton.tsx"
      via: "<Suspense key={activeTab} fallback={<KpiCardsSkeleton />}>"
      pattern: "Suspense.*key=\\{activeTab\\}"
---

<objective>
대시보드 page.tsx를 getTeamDashboardData()로 교체하고, searchParams 기반 Daily/Weekly 탭 분기와 Suspense skeleton을 완성한다. 이후 브라우저에서 전체 플로우를 검증한다.

Purpose: TAB-02(URL searchParams), TAB-03(탭 전환 시 서버 재페칭), UX-01(로딩 스켈레턴)을 page.tsx에서 통합 완성한다. Phase 1에서 허용된 getDashboardData import 오류를 해소한다.
Output: 수정된 app/(dashboard)/dashboard/page.tsx, npm run build 성공
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-shell-kpi-cards/02-01-SUMMARY.md
@.planning/phases/02-dashboard-shell-kpi-cards/02-02-SUMMARY.md
@app/(dashboard)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 대시보드 page.tsx 교체 및 빌드 검증</name>
  <files>app/(dashboard)/dashboard/page.tsx</files>
  <action>
    `app/(dashboard)/dashboard/page.tsx`를 아래 구현으로 완전 교체한다. 기존 getDashboardData import와 스타터킷 컴포넌트 import를 모두 제거한다.

    ```typescript
    // 대시보드 메인 페이지 (Server Component)
    // searchParams로 탭 상태를 읽어 KPI 카드를 렌더링합니다
    import { Suspense } from 'react';
    import { getTeamDashboardData } from '@/lib/data';
    import { KpiCards } from '@/components/dashboard/kpi-cards';
    import { KpiCardsSkeleton } from '@/components/dashboard/kpi-cards-skeleton';
    import { TabNav } from '@/components/dashboard/tab-nav';

    // 탭 전환 시 서버에서 최신 데이터를 가져오도록 캐시 비활성화
    export const dynamic = 'force-dynamic';

    type SearchParams = Promise<{ tab?: string }>;

    export default async function DashboardPage({
      searchParams,
    }: {
      searchParams: SearchParams;
    }) {
      // Next.js 16: searchParams는 Promise — await 필수
      const { tab = 'daily' } = await searchParams;
      const activeTab = tab === 'weekly' ? 'weekly' : 'daily';

      const data = await getTeamDashboardData();

      return (
        <div className="space-y-6">
          {/* 탭 네비게이션 — useSearchParams() 사용으로 Suspense 감싸기 필요 */}
          <Suspense fallback={null}>
            <TabNav activeTab={activeTab} />
          </Suspense>

          {/* key prop으로 탭 전환 시 Suspense 리셋 → 스켈레턴 재표시 (UX-01) */}
          <Suspense key={activeTab} fallback={<KpiCardsSkeleton />}>
            <KpiCards data={data} tab={activeTab} />
          </Suspense>
        </div>
      );
    }
    ```

    중요 사항:
    - `TabNav`는 `useSearchParams()`를 사용하는 Client Component이므로, Suspense boundary 안에 두어야 한다. fallback은 `null`로 설정 (탭 UI는 server-side activeTab으로 초기 렌더 후 hydration 완료).
    - `KpiCards`의 Suspense는 `key={activeTab}`으로 탭 전환마다 재마운트하여 스켈레턴을 재표시한다.
    - 기존 스타터킷 import (`RevenueChart`, `CategoryChart`, `RecentOrdersTable`, `getDashboardData`)는 모두 삭제한다. Phase 5에서 해당 파일들을 정리할 예정이므로 이 시점에서 파일 자체를 삭제하지 않는다.

    교체 후 빌드를 실행한다:
    ```bash
    npm run build
    ```
  </action>
  <verify>
    1. `npm run build` 성공 (exit code 0)
    2. `grep "getDashboardData" app/\(dashboard\)/dashboard/page.tsx` — 출력 없음 (삭제됨)
    3. `grep "force-dynamic" app/\(dashboard\)/dashboard/page.tsx` — 출력 있음
    4. `grep "getTeamDashboardData" app/\(dashboard\)/dashboard/page.tsx` — 출력 있음
  </verify>
  <done>page.tsx가 getTeamDashboardData()를 사용하고, dynamic='force-dynamic'이 설정되고, npm run build가 성공한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 전체 기능 브라우저 검증</name>
  <files>none</files>
  <action>
    Task 1 완료 후 브라우저에서 Phase 2 전체 구현을 검증한다.
    `npm run dev`를 실행하고 아래 how-to-verify 단계를 따른다.
  </action>
  <what-built>
    Phase 2 전체 구현:
    - Daily/Weekly 탭 전환 (URL searchParams ?tab=daily|weekly)
    - 5개 KPI 카드 (매출, 손익, 이용건수, 가동률, 이용시간)
    - Weekly 탭 매출 카드: 달성률(%) + 프로그레스 바 + 조건부 색상
    - 기간 비교 델타: "▲ +12% / ₩120만" 형식
    - 로딩 스켈레턴 (탭 전환 시 재표시)
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 http://localhost:3000/dashboard 접속
    2. Daily 탭 (기본):
       - 5개 KPI 카드가 그리드로 표시되는지 확인
       - 각 카드에 KPI명, 실적값, 델타 텍스트가 표시되는지 확인
       - Daily 카드에 달성률/프로그레스 바가 없는지 확인
       - 델타 색상: 양수 녹색, 음수 빨간색 확인
    3. Weekly 탭 전환:
       - "주차별" 탭 클릭 시 URL이 ?tab=weekly로 변경되는지 확인
       - 탭 전환 시 스켈레턴이 잠깐 표시된 후 카드가 렌더링되는지 확인
       - Weekly 매출 카드에 달성률(%) + 프로그레스 바가 표시되는지 확인
       - 달성률 색상: 80%+ 녹색, 60~80% 주황, 60% 미만 빨간색 확인
    4. URL 공유:
       - http://localhost:3000/dashboard?tab=weekly URL 직접 입력 시 Weekly 탭이 선택된 채로 로드되는지 확인
    5. 다크 모드:
       - 테마 토글로 다크 모드 전환 시 카드 색상이 올바르게 반전되는지 확인
  </how-to-verify>
  <verify>사용자가 "approved"를 입력하거나 발견된 문제를 설명한다</verify>
  <done>사용자가 "approved"를 입력하여 Phase 2 전체 기능이 올바르게 동작함을 확인한다</done>
  <resume-signal>문제 없으면 "approved" 입력. 문제 있으면 구체적인 증상을 설명해주세요.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exit code 0
- `grep "getDashboardData" "app/(dashboard)/dashboard/page.tsx"` — 결과 없음
- `grep "force-dynamic" "app/(dashboard)/dashboard/page.tsx"` — 출력 있음
- `grep "getTeamDashboardData" "app/(dashboard)/dashboard/page.tsx"` — 출력 있음
- 브라우저에서 Daily/Weekly 탭 전환 시 URL 변경 + 스켈레턴 표시 + 데이터 렌더링 확인
</verification>

<success_criteria>
- npm run build 성공 (Phase 1에서 허용된 TypeScript 오류 해소)
- http://localhost:3000/dashboard?tab=daily → Daily 탭, 5개 카드, 달성률 없음
- http://localhost:3000/dashboard?tab=weekly → Weekly 탭, 매출 카드에 달성률 + 프로그레스 바
- 탭 전환 시 URL 변경 + 스켈레턴 재표시
- 달성률 조건부 색상: 80%+녹색/60~80%주황/60%미만빨간 (mock 데이터에 55~92% 분포 포함)
</success_criteria>

<output>
완료 후 `.planning/phases/02-dashboard-shell-kpi-cards/02-03-SUMMARY.md`를 생성한다.
</output>
