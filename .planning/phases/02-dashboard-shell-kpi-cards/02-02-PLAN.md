---
phase: 02-dashboard-shell-kpi-cards
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/dashboard/tab-nav.tsx
  - components/dashboard/kpi-card.tsx
  - components/dashboard/kpi-cards.tsx
autonomous: true
requirements: [TAB-01, TAB-02, KPI-01, KPI-02, KPI-03, KPI-04, KPI-05]
must_haves:
  truths:
    - "TabNav가 Daily/Weekly 탭 버튼을 렌더링하고 클릭 시 URL searchParams에 ?tab=daily|weekly를 반영한다"
    - "KpiCard가 KPI명, 실적값, 목표값, 달성률(%), 프로그레스 바, 델타 텍스트를 표시한다"
    - "달성률 80%+ → 녹색, 60~80% → 주황, 60% 미만 → 빨간 색상이 적용된다"
    - "Daily 탭 카드에는 달성률/프로그레스 바가 숨겨지고 실적값과 델타만 표시된다"
    - "KpiCards가 TeamDashboardData와 activeTab을 받아 5개 KpiCard를 렌더링한다"
  artifacts:
    - path: "components/dashboard/tab-nav.tsx"
      provides: "Daily/Weekly 탭 전환 Client Component"
      contains: "use client"
      exports: [TabNav]
    - path: "components/dashboard/kpi-card.tsx"
      provides: "단일 KPI 카드 컴포넌트"
      exports: [KpiCard]
    - path: "components/dashboard/kpi-cards.tsx"
      provides: "5개 KPI 카드 그리드 Server Component"
      exports: [KpiCards]
  key_links:
    - from: "components/dashboard/tab-nav.tsx"
      to: "next/navigation"
      via: "useRouter, useSearchParams, usePathname"
      pattern: "router\\.replace.*tab="
    - from: "components/dashboard/kpi-cards.tsx"
      to: "lib/kpi-utils.ts"
      via: "calcDelta, calcAchievementRate"
      pattern: "calcDelta|calcAchievementRate"
    - from: "components/dashboard/kpi-card.tsx"
      to: "components/ui/progress.tsx"
      via: "import { Progress }"
      pattern: "Progress.*value="
---

<objective>
Daily/Weekly 탭 전환 Client Component(TabNav)와 KPI 카드 컴포넌트(KpiCard + KpiCards)를 구현한다.

Purpose: TAB-01/TAB-02 URL 탭 상태 관리와 KPI-01~KPI-05 카드 표시 요구사항을 충족한다.
Output: components/dashboard/tab-nav.tsx, components/dashboard/kpi-card.tsx, components/dashboard/kpi-cards.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-layer-foundation/01-02-SUMMARY.md
@.planning/phases/02-dashboard-shell-kpi-cards/02-01-SUMMARY.md
@types/dashboard.ts
@lib/kpi-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabNav Client Component 구현</name>
  <files>components/dashboard/tab-nav.tsx</files>
  <action>
    `components/dashboard/tab-nav.tsx`를 생성한다. `"use client"` 지시어로 시작하는 Client Component다.

    **구현 사항:**
    - `useRouter`, `useSearchParams`, `usePathname` from `next/navigation` 사용
    - shadcn `Tabs`, `TabsList`, `TabsTrigger` from `@/components/ui/tabs` 사용
    - activeTab prop을 받아 현재 선택된 탭을 표시
    - 탭 클릭 시 `router.replace(pathname?tab=value, { scroll: false })` 로 URL 업데이트
    - 탭 레이블: Daily → "일별", Weekly → "주차별"

    ```typescript
    'use client';

    // 탭 전환 Client Component — URL searchParams로 탭 상태 관리
    import { useRouter, useSearchParams, usePathname } from 'next/navigation';
    import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs';

    interface TabNavProps {
      activeTab: 'daily' | 'weekly';
    }

    export function TabNav({ activeTab }: TabNavProps) {
      const router = useRouter();
      const pathname = usePathname();
      const searchParams = useSearchParams();

      function handleTabChange(value: string) {
        const params = new URLSearchParams(searchParams.toString());
        params.set('tab', value);
        // scroll: false — 탭 전환 시 스크롤 위치 유지
        router.replace(`${pathname}?${params.toString()}`, { scroll: false });
      }

      return (
        <Tabs value={activeTab} onValueChange={handleTabChange}>
          <TabsList>
            <TabsTrigger value="daily">일별</TabsTrigger>
            <TabsTrigger value="weekly">주차별</TabsTrigger>
          </TabsList>
        </Tabs>
      );
    }
    ```

    주의: TabNav는 `useSearchParams()`를 사용하므로 Next.js App Router 요구사항에 따라 반드시 Suspense boundary 내에서 렌더링해야 한다. page.tsx에서 Suspense로 감싸거나 TabNav 자체를 Suspense로 감싸는 방식으로 처리한다(02-03에서 처리).
  </action>
  <verify>
    `npx tsc --noEmit --skipLibCheck 2>&1 | grep "tab-nav"` — TypeScript 오류 없음
  </verify>
  <done>components/dashboard/tab-nav.tsx가 존재하고 TabNav 컴포넌트가 export됨. "use client" 포함.</done>
</task>

<task type="auto">
  <name>Task 2: KpiCard 단일 카드 컴포넌트 구현</name>
  <files>components/dashboard/kpi-card.tsx</files>
  <action>
    `components/dashboard/kpi-card.tsx`를 생성한다. 단일 KPI 카드 컴포넌트이며 Server Component다("use client" 없음).

    **Props 인터페이스:**
    ```typescript
    interface KpiCardProps {
      title: string;           // KPI 명칭 (예: "매출")
      value: string;           // 포맷된 실적값 (예: "₩1,234만")
      target?: string;         // 포맷된 목표값 (Weekly 전용, Daily면 undefined)
      achievementRate?: number; // 달성률 % (Weekly 전용, Daily면 undefined)
      deltaText?: string;      // 포맷된 델타 문자열 (예: "▲ +12% / ₩120만")
      deltaColorClass?: string; // 델타 색상 클래스
      icon: React.ReactNode;   // lucide-react 아이콘
    }
    ```

    **렌더링 구조:**
    - shadcn `Card`, `CardHeader`, `CardTitle`, `CardContent` 사용
    - CardHeader: 아이콘(오른쪽) + KPI 제목(왼쪽) (shadcn KPI 카드 표준 레이아웃)
    - CardContent:
      1. 실적값 (큰 숫자, text-2xl font-bold)
      2. achievementRate가 있을 때만: 달성률 텍스트 (`getAchievementColorClass` 적용) + shadcn Progress 바 (`getProgressColorClass` 적용)
      3. target이 있을 때만: "목표: {target}" 서브텍스트 (text-xs text-muted-foreground)
      4. deltaText가 있을 때만: 델타 텍스트 (deltaColorClass 적용, text-xs)

    **달성률 색상:**
    - `lib/kpi-utils.ts`의 `getAchievementColorClass(achievementRate)` 사용
    - `getProgressColorClass(achievementRate)` 를 Progress 컴포넌트의 className에 적용

    **중요:** Progress 값은 `Math.min(achievementRate, 100)`으로 100을 상한으로 설정 (100% 초과 시 바가 넘치지 않도록).

    ```typescript
    // 단일 KPI 카드 컴포넌트 — 달성률, 프로그레스 바, 델타 표시
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
    import { Progress } from '@/components/ui/progress';
    import { cn } from '@/lib/utils';
    import { getAchievementColorClass, getProgressColorClass } from '@/lib/kpi-utils';

    interface KpiCardProps {
      title: string;
      value: string;
      target?: string;
      achievementRate?: number;
      deltaText?: string;
      deltaColorClass?: string;
      icon: React.ReactNode;
    }

    export function KpiCard({
      title,
      value,
      target,
      achievementRate,
      deltaText,
      deltaColorClass,
      icon,
    }: KpiCardProps) {
      return (
        <Card>
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium">{title}</CardTitle>
            <div className="text-muted-foreground">{icon}</div>
          </CardHeader>
          <CardContent className="space-y-2">
            <div className="text-2xl font-bold">{value}</div>
            {achievementRate !== undefined && (
              <>
                <div className={cn('text-xs font-medium', getAchievementColorClass(achievementRate))}>
                  달성률 {achievementRate}%
                </div>
                <Progress
                  value={Math.min(achievementRate, 100)}
                  className={cn('h-2', getProgressColorClass(achievementRate))}
                />
              </>
            )}
            {target && (
              <p className="text-xs text-muted-foreground">목표: {target}</p>
            )}
            {deltaText && (
              <p className={cn('text-xs', deltaColorClass)}>{deltaText}</p>
            )}
          </CardContent>
        </Card>
      );
    }
    ```
  </action>
  <verify>
    `npx tsc --noEmit --skipLibCheck 2>&1 | grep "kpi-card\."` — TypeScript 오류 없음
  </verify>
  <done>components/dashboard/kpi-card.tsx가 존재하고 KpiCard 컴포넌트가 export됨. achievementRate 조건부 렌더링 포함.</done>
</task>

<task type="auto">
  <name>Task 3: KpiCards 그리드 Server Component 구현</name>
  <files>components/dashboard/kpi-cards.tsx</files>
  <action>
    `components/dashboard/kpi-cards.tsx`를 생성한다. TeamDashboardData를 받아 5개 KpiCard를 렌더링하는 Server Component("use client" 없음).

    **구현 로직:**

    **Daily 탭 (activeTab === 'daily'):**
    - `data.daily` 배열을 날짜 오름차순으로 정렬 후 마지막 항목이 최신 일자, 마지막-1 항목이 전일
    - 최신 DailyRecord 없으면 "데이터 없음" 카드 표시
    - 달성률/프로그레스 바 표시 안 함 (DailyRecord에 목표값 없음 — Phase 1 결정)
    - 델타: 전일 대비 증감 (전일 레코드 없으면 deltaText = undefined)

    **Weekly 탭 (activeTab === 'weekly'):**
    - `data.weekly` 배열의 마지막 항목이 이번 주, 마지막-1 항목이 지난 주
    - WeeklyRecord에 weeklyTarget 존재 → 달성률/프로그레스 바 표시
    - 델타: 지난 주 대비 증감

    **5개 KPI 카드 정의 (카드 순서: 비즈니스 중요도 순):**
    1. 매출 (TrendingUp 아이콘, unit: '원')
    2. 손익 (DollarSign 아이콘, unit: '원')
    3. 이용건수 (Users 아이콘, unit: '건')
    4. 가동률 (Activity 아이콘, unit: '%')
    5. 이용시간 (Clock 아이콘, unit: '시간')

    ```typescript
    // KPI 카드 그리드 — TeamDashboardData에서 5개 KPI 카드 렌더링
    import { TrendingUp, DollarSign, Users, Activity, Clock } from 'lucide-react';
    import type { TeamDashboardData } from '@/types/dashboard';
    import {
      calcAchievementRate,
      calcDelta,
      formatKpiValue,
      formatDelta,
      getDeltaColorClass,
    } from '@/lib/kpi-utils';
    import { KpiCard } from './kpi-card';

    interface KpiCardsProps {
      data: TeamDashboardData;
      tab: 'daily' | 'weekly';
    }

    export function KpiCards({ data, tab }: KpiCardsProps) {
      if (tab === 'daily') {
        // 날짜 오름차순 정렬 후 최신/전일 추출
        const sorted = [...data.daily].sort((a, b) => a.date.localeCompare(b.date));
        const current = sorted[sorted.length - 1];
        const previous = sorted[sorted.length - 2];

        if (!current) {
          return <p className="text-muted-foreground">일별 데이터가 없습니다.</p>;
        }

        // 카드 정의 배열
        const cards = [
          {
            title: '매출',
            value: formatKpiValue(current.revenue, '원'),
            delta: previous ? calcDelta(current.revenue, previous.revenue) : null,
            unit: '원' as const,
            icon: <TrendingUp className="h-4 w-4" />,
          },
          {
            title: '손익',
            value: formatKpiValue(current.profit, '원'),
            delta: previous ? calcDelta(current.profit, previous.profit) : null,
            unit: '원' as const,
            icon: <DollarSign className="h-4 w-4" />,
          },
          {
            title: '이용건수',
            value: formatKpiValue(current.usageCount, '건'),
            delta: previous ? calcDelta(current.usageCount, previous.usageCount) : null,
            unit: '건' as const,
            icon: <Users className="h-4 w-4" />,
          },
          {
            title: '가동률',
            value: formatKpiValue(current.utilizationRate, '%'),
            delta: previous ? calcDelta(current.utilizationRate, previous.utilizationRate) : null,
            unit: '%' as const,
            icon: <Activity className="h-4 w-4" />,
          },
          {
            title: '이용시간',
            value: formatKpiValue(current.usageHours, '시간'),
            delta: previous ? calcDelta(current.usageHours, previous.usageHours) : null,
            unit: '시간' as const,
            icon: <Clock className="h-4 w-4" />,
          },
        ];

        return (
          <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">
            {cards.map((card) => (
              <KpiCard
                key={card.title}
                title={card.title}
                value={card.value}
                icon={card.icon}
                deltaText={card.delta ? formatDelta(card.delta.percent, card.delta.absolute, card.unit) : undefined}
                deltaColorClass={card.delta ? getDeltaColorClass(card.delta.percent) : undefined}
              />
            ))}
          </div>
        );
      }

      // Weekly 탭
      const current = data.weekly[data.weekly.length - 1];
      const previous = data.weekly[data.weekly.length - 2];

      if (!current) {
        return <p className="text-muted-foreground">주차별 데이터가 없습니다.</p>;
      }

      const cards = [
        {
          title: '매출',
          value: formatKpiValue(current.revenue, '원'),
          target: formatKpiValue(current.weeklyTarget, '원'),
          achievementRate: calcAchievementRate(current.revenue, current.weeklyTarget),
          delta: previous ? calcDelta(current.revenue, previous.revenue) : null,
          unit: '원' as const,
          icon: <TrendingUp className="h-4 w-4" />,
        },
        {
          title: '손익',
          value: formatKpiValue(current.profit, '원'),
          target: undefined as string | undefined,
          achievementRate: undefined as number | undefined,
          delta: previous ? calcDelta(current.profit, previous.profit) : null,
          unit: '원' as const,
          icon: <DollarSign className="h-4 w-4" />,
        },
        {
          title: '이용건수',
          value: formatKpiValue(current.usageCount, '건'),
          target: undefined as string | undefined,
          achievementRate: undefined as number | undefined,
          delta: previous ? calcDelta(current.usageCount, previous.usageCount) : null,
          unit: '건' as const,
          icon: <Users className="h-4 w-4" />,
        },
        {
          title: '가동률',
          value: formatKpiValue(current.utilizationRate, '%'),
          target: undefined as string | undefined,
          achievementRate: undefined as number | undefined,
          delta: previous ? calcDelta(current.utilizationRate, previous.utilizationRate) : null,
          unit: '%' as const,
          icon: <Activity className="h-4 w-4" />,
        },
        {
          title: '이용시간',
          value: formatKpiValue(current.usageHours, '시간'),
          target: undefined as string | undefined,
          achievementRate: undefined as number | undefined,
          delta: previous ? calcDelta(current.usageHours, previous.usageHours) : null,
          unit: '시간' as const,
          icon: <Clock className="h-4 w-4" />,
        },
      ];

      return (
        <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">
          {cards.map((card) => (
            <KpiCard
              key={card.title}
              title={card.title}
              value={card.value}
              target={card.target}
              achievementRate={card.achievementRate}
              icon={card.icon}
              deltaText={card.delta ? formatDelta(card.delta.percent, card.delta.absolute, card.unit) : undefined}
              deltaColorClass={card.delta ? getDeltaColorClass(card.delta.percent) : undefined}
            />
          ))}
        </div>
      );
    }
    ```

    **중요사항:**
    - Weekly 탭에서 매출 카드만 weeklyTarget 대비 달성률을 표시한다. 손익/이용건수/가동률/이용시간은 목표값이 없으므로 달성률 없이 실적값과 델타만 표시한다.
    - DailyRecord 필드명: `date`, `revenue`, `profit`, `usageHours`, `usageCount`, `utilizationRate`
    - WeeklyRecord 필드명: `weekNumber`, `revenue`, `profit`, `usageHours`, `usageCount`, `utilizationRate`, `weeklyTarget`
    - types/dashboard.ts 파일을 먼저 확인하여 실제 필드명을 검증한 후 구현한다
  </action>
  <verify>
    `npx tsc --noEmit --skipLibCheck 2>&1 | grep "kpi-cards\."` — TypeScript 오류 없음
  </verify>
  <done>components/dashboard/kpi-cards.tsx가 존재하고 KpiCards 컴포넌트가 export됨. Daily/Weekly 분기 로직 포함. TypeScript 오류 없음.</done>
</task>

</tasks>

<verification>
- `ls components/dashboard/tab-nav.tsx components/dashboard/kpi-card.tsx components/dashboard/kpi-cards.tsx` — 3개 파일 존재
- `npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "tab-nav|kpi-card"` — 해당 파일들 TypeScript 오류 없음
- tab-nav.tsx 최상단에 `'use client'` 포함 확인
- kpi-cards.tsx에 `"use client"` 없음 확인 (Server Component)
</verification>

<success_criteria>
- TabNav: "use client" 포함, useRouter + useSearchParams로 URL ?tab= 업데이트
- KpiCard: achievementRate 조건부로 달성률 텍스트 + Progress 바 표시, getAchievementColorClass/getProgressColorClass 적용
- KpiCards: Daily 탭은 달성률 없이 실적값 + 델타만, Weekly 탭 매출 카드만 weeklyTarget 대비 달성률 표시
- 5개 카드 그리드: xl:grid-cols-5 반응형
- TypeScript strict 모드 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/02-dashboard-shell-kpi-cards/02-02-SUMMARY.md`를 생성한다.
</output>
