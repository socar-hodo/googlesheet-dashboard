---
phase: 04-data-table-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/(dashboard)/dashboard/page.tsx
autonomous: false
requirements:
  - TABLE-01
  - TABLE-02
  - TABLE-03
  - UX-03

must_haves:
  truths:
    - "page.tsx에 DataTable과 DataTableSkeleton이 Suspense로 감싸져 렌더링된다"
    - "page.tsx에 UpdateTimestamp가 렌더링되어 마지막 업데이트 시각이 표시된다"
    - "브라우저에서 Daily/Weekly 탭 전환 시 테이블이 올바른 데이터로 표시된다"
    - "테이블에 striped 행, sticky 헤더, 합계/평균 요약 행이 보인다"
    - "타임스탬프가 대시보드 상단 우측에 표시된다"
  artifacts:
    - path: "app/(dashboard)/dashboard/page.tsx"
      provides: "DataTable + UpdateTimestamp 통합된 대시보드 메인 페이지"
      contains: "DataTable|UpdateTimestamp|DataTableSkeleton"
  key_links:
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/data-table.tsx"
      via: "import DataTable + DataTableSkeleton, Suspense 감싸기"
      pattern: "DataTable|DataTableSkeleton"
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/update-timestamp.tsx"
      via: "import UpdateTimestamp, data.fetchedAt prop 전달"
      pattern: "UpdateTimestamp.*fetchedAt|fetchedAt.*UpdateTimestamp"
---

<objective>
page.tsx 통합 — DataTable과 UpdateTimestamp를 대시보드 메인 페이지에 연결하고 브라우저 검증

Purpose: 테이블과 타임스탬프가 실제 대시보드에서 올바르게 렌더링되는지 사용자가 확인한다.
Output: 완성된 Phase 4 대시보드 (테이블 + 타임스탬프 포함)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-table-polish/04-CONTEXT.md
@app/(dashboard)/dashboard/page.tsx
@.planning/phases/04-data-table-polish/04-01-SUMMARY.md
@.planning/phases/04-data-table-polish/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx에 DataTable + UpdateTimestamp 통합</name>
  <files>app/(dashboard)/dashboard/page.tsx</files>
  <action>
    기존 page.tsx를 수정한다. 현재 파일은 TabNav, KpiCards, ChartsSection 세 섹션으로 구성됨.

    **추가할 import:**
    ```typescript
    import { DataTable } from '@/components/dashboard/data-table';
    import { DataTableSkeleton } from '@/components/dashboard/data-table-skeleton';
    import { UpdateTimestamp } from '@/components/dashboard/update-timestamp';
    ```

    **UpdateTimestamp 위치 (CONTEXT.md 결정: 대시보드 상단 우측):**
    - TabNav Suspense 바로 아래, KpiCards Suspense 위에 배치
    - `<div className="flex justify-end">` 로 감싸 우측 정렬
    - `<UpdateTimestamp fetchedAt={data.fetchedAt} />`

    **DataTable Suspense 블록 (ChartsSection 아래에 추가):**
    ```tsx
    <Suspense key={`table-${activeTab}`} fallback={<DataTableSkeleton />}>
      <DataTable data={data} tab={activeTab} />
    </Suspense>
    ```
    - `key={`table-${activeTab}`}` — 탭 전환 시 스켈레턴 재표시 (UX-01 패턴 준수)

    **최종 page.tsx 섹션 순서:**
    1. TabNav Suspense
    2. UpdateTimestamp (div.flex.justify-end로 우측 정렬)
    3. KpiCards Suspense
    4. ChartsSection Suspense
    5. DataTable Suspense (신규 추가)

    기존 코드의 주석 스타일(// 한국어 주석)과 포맷을 유지한다.
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -15</automated>
    <manual>빌드 성공 확인</manual>
  </verify>
  <done>page.tsx가 빌드 성공하고 DataTable, DataTableSkeleton, UpdateTimestamp import가 포함됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 브라우저 검증 — 테이블 렌더링 및 타임스탬프 표시 확인</name>
  <what-built>
    Phase 4 완성: DataTable (Daily/Weekly), UpdateTimestamp (상단 우측), DataTableSkeleton (로딩 스켈레턴) 통합 완료.
    `npm run dev`로 localhost:3000 접속 후 확인 필요.
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 http://localhost:3000/dashboard 접속
    2. Daily 탭 확인:
       - 날짜/매출/손익/이용시간/이용건수/가동률 6컬럼 테이블 표시 여부
       - 홀짝 행 배경색 교체(striped) 확인
       - 테이블 하단 합계/평균 2행이 굵은 글씨 + 진한 배경으로 표시 여부
       - 손익 음수 값이 빨간색 텍스트 확인
       - 이용시간 "Xh Ym" 형식 확인
       - 가동률 소수점 1자리 % 확인
    3. Weekly 탭 확인:
       - 주차/매출/손익/이용시간/이용건수/가동률/목표 7컬럼 테이블 표시 여부
       - 합계/평균 요약 행 확인
    4. 상단 우측 타임스탬프 확인:
       - "마지막 업데이트: N분/시간 전 (YYYY. MM. DD. HH:mm)" 형식 표시 여부
    5. 다크 모드 전환 후 테이블 색상 확인 (striped 배경, 손익 빨간색 등)
    6. 테이블이 길면 상단 스크롤 시 헤더 sticky 고정 확인
  </how-to-verify>
  <resume-signal>브라우저 확인 후 "approved" 입력, 또는 발견된 문제 설명</resume-signal>
  <action>npm run dev 실행 후 브라우저에서 http://localhost:3000/dashboard 접속하여 위 how-to-verify 항목을 순서대로 확인한다.</action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -5</automated>
    <manual>브라우저에서 Daily/Weekly 탭 테이블 렌더링, 타임스탬프 표시, 다크모드 색상 정상 확인</manual>
  </verify>
  <done>테이블과 타임스탬프가 라이트/다크 양 테마에서 올바르게 렌더링되고, Daily/Weekly 탭 전환 시 올바른 데이터로 표시된다</done>
</task>

</tasks>

<verification>
npm run build TypeScript 에러 없이 성공
page.tsx에 DataTable, UpdateTimestamp import 포함
Suspense key에 activeTab 포함하여 탭 전환 시 스켈레턴 재표시
브라우저에서 Daily/Weekly 테이블과 타임스탬프 정상 렌더링 사용자 확인
</verification>

<success_criteria>
- npm run build 성공
- Daily 탭: 6컬럼 테이블 + 합계/평균 요약 2행
- Weekly 탭: 7컬럼 테이블 + 합계/평균 요약 2행
- 타임스탬프 "마지막 업데이트: N시간 전 (절대시각)" 형식으로 상단 우측 표시
- 손익 음수 빨간색, 이용시간 Xh Ym, 가동률 소수점 1자리 %
- striped 행, sticky 헤더, 요약 행 bold+muted 배경
- 사용자 브라우저 검증 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-table-polish/04-03-SUMMARY.md`
</output>
