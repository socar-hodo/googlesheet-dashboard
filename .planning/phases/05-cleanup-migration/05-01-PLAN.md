---
phase: 05-cleanup-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/revenue-chart.tsx
  - components/dashboard/category-chart.tsx
  - components/dashboard/recent-orders-table.tsx
autonomous: false
requirements:
  - UX-04

must_haves:
  truths:
    - "레거시 스타터킷 컴포넌트 3개 파일이 파일시스템에 존재하지 않는다"
    - "삭제 후 npm run build가 오류 없이 성공한다"
    - "대시보드가 팀 전용 컴포넌트만으로 정상 동작한다"
  artifacts:
    - path: "components/dashboard/revenue-chart.tsx"
      provides: "삭제 확인 대상 - 파일이 존재하지 않아야 함"
      contains: "DELETED"
    - path: "components/dashboard/category-chart.tsx"
      provides: "삭제 확인 대상 - 파일이 존재하지 않아야 함"
      contains: "DELETED"
    - path: "components/dashboard/recent-orders-table.tsx"
      provides: "삭제 확인 대상 - 파일이 존재하지 않아야 함"
      contains: "DELETED"
  key_links:
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/ 팀 전용 컴포넌트"
      via: "import 목록"
      pattern: "KpiCards|ChartsSection|DataTable|UpdateTimestamp"
---

<objective>
스타터킷에서 가져온 범용 컴포넌트 3개(revenue-chart, category-chart, recent-orders-table)를 삭제하고, 빌드 성공으로 팀 전용 대시보드 완성을 검증한다.

Purpose: Phase 1~4에서 팀 전용 컴포넌트로 완전 교체가 이루어졌으나 레거시 파일이 파일시스템에 잔류한다. 이를 제거하여 코드베이스를 팀 전용 컴포넌트만으로 완성한다.
Output: 3개 레거시 파일 삭제 + 빌드 성공 확인
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup-migration/05-CONTEXT.md
@.planning/phases/05-cleanup-migration/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 레거시 스타터킷 컴포넌트 3개 삭제</name>
  <files>
    components/dashboard/revenue-chart.tsx
    components/dashboard/category-chart.tsx
    components/dashboard/recent-orders-table.tsx
  </files>
  <action>
    삭제 전 import 참조 재확인 (연구에서 이미 확인됨, 안전망 목적):

    grep -r "RevenueChart\|CategoryChart\|RecentOrdersTable\|revenue-chart\|category-chart\|recent-orders-table" --include="*.tsx" --include="*.ts" components/ app/ lib/

    결과에 삭제 대상 3개 파일 외의 파일이 나오면 해당 파일의 import 줄만 먼저 제거한다.
    결과가 없거나 3개 파일만 나오면 정상이므로 삭제를 진행한다.

    3개 파일 일괄 삭제 (git rm으로 삭제+스테이징 동시):
    git rm components/dashboard/revenue-chart.tsx components/dashboard/category-chart.tsx components/dashboard/recent-orders-table.tsx

    주의사항:
    - lib/ 파일(data.ts, sheets.ts, mock-data.ts)은 건드리지 않는다 (CONTEXT.md 결정)
    - components/ui/ shadcn 컴포넌트는 유지
    - components/dashboard/charts/ 하위 팀 전용 차트는 유지
    - 다른 파일에 TODO(Phase 5) 주석이 발견되면 주석 줄만 제거, 코드 블록은 유지
    - 범위 확장 금지: 3개 파일 삭제 외의 작업을 수행하지 않는다
  </action>
  <verify>
    <automated>test ! -f components/dashboard/revenue-chart.tsx &amp;&amp; test ! -f components/dashboard/category-chart.tsx &amp;&amp; test ! -f components/dashboard/recent-orders-table.tsx &amp;&amp; echo "3개 파일 삭제 완료" || echo "삭제 실패"</automated>
  </verify>
  <done>3개 레거시 파일이 파일시스템에 존재하지 않고, git status에 3개 삭제가 staged 상태이다</done>
</task>

<task type="auto">
  <name>Task 2: 빌드 검증 및 커밋</name>
  <files></files>
  <action>
    파일 삭제 후 프로덕션 빌드를 실행하여 import 참조 오류가 없는지 확인한다:

    npm run build

    기대 출력:
    - Compiled successfully
    - Generating static pages (6/6)

    빌드 실패 시:
    - 오류 메시지에서 누락된 import 참조를 찾는다
    - 오류가 삭제한 파일 관련이면 해당 import 참조하는 파일에서 그 줄을 제거한다
    - 수정 후 npm run build 재실행
    - 빌드 성공까지 반복한다

    빌드 성공 후 git commit:
    git add -A
    git commit -m "feat(05-01): 레거시 스타터킷 컴포넌트 3개 삭제 (UX-04)"
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -E "Compiled|compiled|error TS" | tail -5</automated>
    <manual>빌드 출력에 Compiled successfully 메시지가 있고 TypeScript 오류가 없는지 확인</manual>
  </verify>
  <done>npm run build가 TypeScript/import 오류 없이 완료된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 대시보드 동작 최종 확인</name>
  <what-built>
    레거시 스타터킷 컴포넌트 3개 삭제 완료 및 빌드 성공 검증:
    - components/dashboard/revenue-chart.tsx 삭제
    - components/dashboard/category-chart.tsx 삭제
    - components/dashboard/recent-orders-table.tsx 삭제
    - npm run build 성공
  </what-built>
  <how-to-verify>
    1. npm run dev 실행 후 http://localhost:3000 접속
    2. 대시보드가 정상 로드되는지 확인 (KPI 카드, 차트, 데이터 테이블 모두 표시)
    3. Daily 탭과 Weekly 탭 전환이 정상 동작하는지 확인
    4. 브라우저 콘솔에 컴포넌트 관련 오류가 없는지 확인
  </how-to-verify>
  <resume-signal>approved 입력 시 Phase 5 완료 처리. 이슈 발견 시 구체적 오류 내용 전달.</resume-signal>
  <action>npm run dev 실행 후 http://localhost:3000 접속하여 above how-to-verify 항목을 순서대로 확인한다.</action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -3</automated>
    <manual>브라우저에서 대시보드가 KPI 카드, 차트, 데이터 테이블 모두 정상 표시되는지 확인</manual>
  </verify>
  <done>대시보드가 팀 전용 컴포넌트만으로 Daily/Weekly 탭 모두 정상 동작하고 콘솔 오류가 없다</done>
</task>

</tasks>

<verification>
- test ! -f components/dashboard/revenue-chart.tsx 등 3개 파일 존재 여부 확인
- npm run build 성공 여부 확인
- grep으로 RevenueChart|CategoryChart|RecentOrdersTable import 참조 없음 확인
</verification>

<success_criteria>
1. revenue-chart.tsx, category-chart.tsx, recent-orders-table.tsx가 파일시스템에 존재하지 않는다
2. npm run build가 TypeScript/import 오류 없이 성공한다
3. 대시보드가 http://localhost:3000에서 팀 전용 컴포넌트만으로 정상 동작한다
</success_criteria>

<output>
After completion, create .planning/phases/05-cleanup-migration/05-01-SUMMARY.md

Summary must include:
- Files deleted: revenue-chart.tsx, category-chart.tsx, recent-orders-table.tsx
- Build output snippet (Compiled successfully 메시지 포함)
- Final components/dashboard/ structure (팀 전용 컴포넌트만 남은 상태)
</output>
